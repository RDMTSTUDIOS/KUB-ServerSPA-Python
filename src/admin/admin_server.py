from socket import socket
 


def CreateAdminServer(admin_server_config) -> None:

    # Returns not found responce to socket and closes it
    def send_not_found() -> None:
        return

    # Starts admin server and makes it opened to connections
    def raise_server() -> None:
        return
         
    return